{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite --port=3001 --host=0.0.0.0",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
